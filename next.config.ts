import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // This starter needs server behavior for callback/session cookies.
  // Do not set output: "export".
};

export default nextConfig;
